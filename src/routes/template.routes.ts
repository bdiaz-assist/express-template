import { Router } from 'express';
const router = Router();

/* -------------------------   Route Template  -------------------------

import { getCustomersByParameters } from '../controllers/customers.controller';

router.route('/customers/:identificationCountry/:identificationType/:numberIdentification')
    .get(getCustomersByParameters)  

*/

export default router; 